# D21-v3参考源审查结果

状态：`DATA_EVIDENCE_INCOMPLETE`，尚未启动正式Stage3R/4R。覆盖门槛仍为85%，已完成预检覆盖89.76%；两种门禁不能互相替代。

证据：`experiments/d21_v3_reference_source_audit_20260914/report.json`。入口：`scripts/audit_d21_v3_reference_source.py`，拒绝覆盖历史审查。

## 已通过的内容检查

1. 补源文件与原冻结报告的SHA256全部一致。
2. 从历史区间重建后，每个研究日中证500成员均为500只。
3. 新增科技成员日可以由指数、行业有效区间及沪深300排除规则完整重建。
4. 新增行业代码限定电子、计算机、通信。
5. 缩短研究日历后的历史前缀成员记录一致。这不是对供应商历史修订或真实公告时间的独立证明。
6. 由原始日线及PRECLOSE重建的参考收益与保存输入一致。
7. 7027个新增参考股停牌成员日均没有有限收益，不存在本次检查所针对的停牌收益填0。

新增参考股研究期内共194只、173896个成员日。未读取标签、PnL或Holdout。

## 未满足的证据要求

- 补源包没有逐请求`raw_request_manifest`。现有plan、完成标记和文件哈希不能替代请求ID、请求/完成时间、SDK版本和逐批参数等原数据字典要求。检索数据项目文件清单也未发现对应manifest/request/metadata文件；不能断言其他位置一定没有日志。
- 参考输入仍为实验中间表，缺少正式`available_at`及来源枚举、行级来源等元数据，不能直接作为已通过正式门禁的数据快照。
- 原生公告时间尚未独立验证只作披露：原D05/D06允许保守的生效日及当日21:00派生策略，必须标记`DERIVED_POLICY`，绝不能把生效日冒充原生公告时间。没有为v3新增“必须取得原生公告”的硬门槛。

## 后续修复顺序

先恢复本次真实请求日志；若无法恢复，则新建Development范围的补采版本并在采集时记录完整请求清单。下载时间只能记录真实的新采集时间，不从文件修改时间猜测旧请求时间。

随后按原D05/D06生成独立的规范化参考数据及行级追溯，保留所有旧文件。新下载结果若与本次源不同，须披露差异并重算同一冻结公式的覆盖率；不得挑选更高覆盖的数据版本。

完成上述源审查后才能接入A–D账本和Stage4R。旧v1/v2结论、85%覆盖值、经济阈值与Stage5关闭状态保持不变。
