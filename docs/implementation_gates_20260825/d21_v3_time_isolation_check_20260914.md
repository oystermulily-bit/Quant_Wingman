# D21-v3时间与隔离专项检测

范围：历史成员/行业生效日、行情可用时间、禁止未来回填、Holdout隔离。只新增检测脚本与证据，未修改生产逻辑、冻结阈值或旧实验；未读取真实Holdout价格、标签或PnL。

结果文件：`experiments/d21_v3_time_isolation_check_20260914/report.json`。
复现入口：`scripts/check_d21_v3_time_isolation.py`。已有相关单测复跑10项通过；额外对抗检测发现两个现有单测未覆盖的时点防护缺口，因此不能表述为“全部时间防护通过”。

## 1. 历史成员与行业：生效日边界通过，时刻精度存在缺口

- 合成边界检查确认：生效前不进入，退出日按既有含尾区间纳入，退出后/行业间隙不沿用旧归属，未来新增行业不回填历史。
- 真实来源在上一轮审查中完整重建：每日中证500恰好500，新增科技股173896成员日与存档一致。该结果仅证明有效区间逻辑，不独立证明供应商未事后修订分类。
- 原沪深300行业表993行known_at均有值，均标为DERIVED_POLICY，未发现晚于生效日21点的值；这些不是原生公告时间。
- 对抗检测发现：`data_pipeline/hs300_panel.py:772`把known_at归一为日期，781行只比较日期。模拟当日23:00才可得的行业信息仍被同日连接接受，不能保证21:00信号时已知。当前真实行业表采用派生21点，未据此发现实际晚到行业记录。

## 2. 行情可用时间：旧表符合派生策略，新参考源未闭环

- 原成员行情777300行、执行行情1541187行available_at均非空，均为DERIVED_POLICY；无晚于当日21点信号时刻的记录。
- 新参考股8批原始行情均缺available_at。kline_time是行情日期/时间，不能自动等同供应商可得时间。
- 对抗检测发现：`research_d21_v3_reference_only.py:17`的构建器不检查available_at；即使将一只参考股票的可得时间推迟到次日，其当日收益仍影响目标残差。主入口52行也没有读取该字段。现有构建依赖未被代码落实的上游时点保证。
- 这是合成延迟输入暴露的保护缺口，不代表已经证明真实回测读到了未来行情。

## 3. 禁止未来回填：已测窗口及缺失规则通过

- 现有未来收益哨兵、前缀不变性、缺失历史、目标入池前不预热、缺一不可平均等测试通过。
- 改动未来行业区间内容不影响截止日之前的成员连接。历史区间空隙未ffill/bfill。
- 算式中的fillna(0)用于分组LOO代数和n=0分支，不代表对原始收益或未知行业填0；缺失仍受eligible掩码约束。
- 上述检查不替代第1/2项的真实available_at/known_at约束：日历因果与信息可得性是两件事。

## 4. Holdout隔离：当前文件与应用层检查通过

- 锁文件与原split的起点、数量、日期哈希一致：Holdout从2024-08-26开始，483个交易日。
- 对标准化行情、执行行情/状态、交易状态、原成员表及v3输入/输出共8个文件只扫描日期列；无空日期、无Holdout日期，Development截至2024-08-23。
- 在临时合成快照中验证标签越界拒绝、价格物理分目录和无效令牌拒绝；测试读取的合成Holdout不是研究Holdout。
- 本轮只读真实锁元数据，未解封、未读取真实Holdout价格/标签。不声称已经审计整个历史访问日志或实现操作系统层面不可访问。

## 后续需要的修复（本轮未实施）

按既有D05/D06显式生成/传递DERIVED_POLICY可得时间与来源；行业known_at使用完整带时区时刻与信号截止时刻比较；参考收益在进入LOO前校验available_at，缺失/晚到数据明确失败或按已冻结缺失规则处理。补齐来源追溯后复跑本专项检测、严格覆盖预检和数据门禁，才启动Stage3R/4R。不得仅把报告标志改成通过。
